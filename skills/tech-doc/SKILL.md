---
name: tech-doc
description: |
  技术文档 / 技术方案 / 技术教程写作规范：体裁选择（札记 / 方案 / 教程）、
  事实核对到源码、目录设计、专业语域、Mermaid 图表、带解释注释的代码片段、
  发文前自检与符号巡检（scripts/check_symbols.sh）。
  触发：用户要求 写 / 重写 / 梳理 / 压缩 / 润色 / 评审 任何技术文档——
  技术方案、设计文档、实现方案、札记、梳理、教程、接入指南、how-to、README、ADR——
  或询问「文档怎么写」「目录怎么设计」「文档规范」；
  哪怕只说「帮我整理成文档」「把这个方案落成文档」「写个说明」也应触发。
  Compatible with Claude Code, ZCode, openClaw, Codex, and other Agent Skills-compatible agents.
license: MIT
compatibility: Requires file read/write access and shell tools (bash, git, grep, sed, find; Git Bash on Windows) for scripts/check_symbols.sh. Compatible with Claude Code, ZCode, openClaw, Codex, and other Agent Skills-compatible agents.
metadata:
  author: zzy-cl
  version: "1.0.0"
  openclaw:
    emoji: "📝"
    requires:
      bins:
        - bash
        - git
allowed-tools: read write edit exec
---

# 技术文档写作

一套经实战校准的写作规范。产出目标：**体裁正确、每句话都挂在读者的动作上、事实可溯源**的文档。

核心认知（所有规则的出处，执行中拿不准时回到这里）：

- 文档的价值 = 它改变读者多少动作和判断；读者的成本按**每行的认知税**结算，不按总字数。
- 读者是扫读的，不是阅读的：**容器（分条 / 表格 / 代码 / 图）是写作的基本单位**，散文只负责粘合；长段落把结构提取的成本转嫁给读者。
- 裁剪和压缩是两个操作：裁剪按动作判据（不写会做错才留），压缩按容器选择（装进信息形状对应的最紧容器）。简洁是两者的结果，不是目标。
- 文档的失败几乎总是"多"而不是"缺"：误删 ≈ 一人次提问，误留 = 每个读者每次阅读付费。**偏向删除。**
- 文档是真相的缓存：必须可溯源、能让失效被发现；溯源靠可验算的内容与留痕位置，不靠头部元信息。

**技能根目录**：本文件所在目录即技能根目录，`references/` 与 `scripts/` 相对它定位。不同 Agent 加载技能的机制不同（有的自动注入本文件，有的按指令手动读取）；路径解析失败时，按此相对关系直接读取文件。

## 入口判断

| 用户意图 | 路径 |
|---|---|
| 新写一篇 | 走完整工作流（下文第 1–6 步） |
| 重写/梳理已有文档 | 先读 `references/antipatterns.md`，按其诊断旧文；再从第 1 步走完整工作流（事实仍需重新核对，不能沿用旧文说法） |
| 评审一篇文档 | 只做第 2 步的事实核对 + 第 6 步的 checklist，输出问题清单（按"致命/应修/建议"分级），不代用户改写 |
| 压缩一篇文档 | 只做第 5 步：逐句判"挂在哪个动作上"，输出删减对照表，保留原文事实不新增内容 |

## 第 1 步：定体裁

动笔前回答三个问题：**读者是谁？读完要能做什么？读前读后的状态差在哪？**答案决定体裁，体裁决定详略。判断不了就问用户一次，不要猜。

| 体裁 | 读者时刻 | 骨架（按序） | 判生死的一条 |
|---|---|---|---|
| 札记（技术文档/梳理） | 几分钟建立心智模型 | 背景（2–4 行）→ 总图 → 机制节×N（按数据流）→ 场景×N | 读者能否拿文中样例值对着日志或代码验算 |
| 方案（设计文档） | 评审，要下判断 | 背景与问题 → 目标与**非目标** → 方案总览（图）→ 分述（按决策点/数据流）→ 备选与取舍 → 风险与验证 | 非目标和被否决项写了没有 |
| 教程（how-to） | 学习中，手把手 | 前置条件 → 目标成果 → 编号步骤 → 常见问题 | 干净环境照做能否一次成功 |

选定体裁后，**先读对应的 `references/genre-*.md` 再动笔**（每次只读选中的那一份）。

## 第 2 步：核对事实（动笔前，不省略）

文档的准确性靠这一步，不靠写得小心。工作方法：

1. **列出事实清单**：把文档将要声称的事实逐条列出——重点抓两类高危句：**数字**（间隔、上限、字节位、版本号）和**全称量词**（"所有""总是""唯一""必须"）。感觉越确定的句子越要核，确定感会短路核对。
2. **按源头分级取数**，每条事实只从唯一权威源取：

| 声明类型 | 唯一权威源 | 不可作源 |
|---|---|---|
| 系统行为 | 代码本身 + 运行观测 | 其他文档、自己的记忆 |
| 历史与决策 | commit message、ADR、评审记录 | 口口相传 |
| 协议约定 | proto 文件、后端接口文档、抓包 | 前端映射表里的注释 |
| 参数与常量 | 常量定义处 | 出现该常量的使用方 |

3. **证伪式核对**：对每条规则问"什么情况下它是错的？"，再到代码里找反例（如：某映射规律是否对所有条目成立）。
4. **三态标注**：事实 / 推断 / 示意 三种状态在文中可区分。示例值标"示意"；推断不写成断言；**无法核实的直接不写**（宁缺毋错——错的比缺的更糟，缺了会被问出来，错了会被信进去）。
5. **核对留痕不进正文**：基线（对照日期 + commit）记录在 commit message / PR 描述；正文零元信息，第一行就是问题本体。仅当文档本身是长期真相源且事实无代码可溯源时，才在头部用一行标注。
6. 记住：**代码注释也只是提示，不是真相**。注释说 8 个市场、联合类型写着 9 个时，以代码为准。

## 第 3 步：排目录

目录是主线的投影。检验：**只读标题连起来，能听懂整篇讲什么、按什么顺序。**

- **两级封顶，7±2 节**（含场景节，一屏放得下）。内容装不下时拆文档（每个新文档重新定体裁），不加层级。
- **兄弟节颗粒一致、语法平行**：同一级标题同属一个缩放级别、同为名词短语。读者靠对称性建立速度预期。
- **标题即检索词**：领域术语必须出现在标题里；标题是一周后读者搜索的入口。
- **文件名同源**：文件名是标题的检索压缩形态，命名公式与禁止项见 `references/naming.md`。
- **顺序跟随依赖**：每节只使用在它之前定义的概念；每节脱离上下文单独发给别人能看懂。
- **标题通读测试**：念给一个不懂该系统的人，能否复述出文档讲什么。通不过，回主线重排。
- 这个体量的文档**标题即目录**：不手写目录页，不写"附：文件索引/名词速查/函数速查"（静态索引默认腐烂，归宿是 ADR 和代码注释）。

## 第 4 步：成稿

**先选容器，再写句子。**读者是扫读的：写作的基本单位是容器，不是段落。每条信息先判形状，装进对应容器；散文只负责粘合与一两行的断言——超过三行的段落是异味，处理方式是换容器而不是删内容。

| 信息形状 | 容器 | 反例 |
|---|---|---|
| 并列事实、步骤、要点 | 分条，粗体引导词开头 | 「首先…其次…同时…」一段带过 |
| 多维对比、映射、取舍 | 表格 | 散文里「而 A 则…，B 则…」 |
| 确定性逻辑、协议、命令 | 代码块 | 文字转述代码（必然漂移） |
| 拓扑、分支、生命周期 | 图（Mermaid 默认：可版本化、可 diff） | 一维顺序也画图（装饰图） |
| 时序推演、状态迁移 | 带具体值的时序块 | 「随后…最终…」的叙述 |
| 结论、判断、边界 | ≤2 行短段或引用块 | 多段论证 |
| 粘合与过渡 | 1–2 行散文 | 成段铺陈 |

**演优于讲。**机制用可执行痕迹承载——流程块、时序、决策表；散文只负责粘合。解说句（"X 是全体系的核心约束"）要求读者先把文字编译成心智模型，流程块直接给出模型。流程块自带三个红利：每个箭头是可证伪的断言、只有一条阅读顺序、嵌进样例值后读者可拿日志验算。

**规则嵌位，不开清单。**铁律写进它会被违反的那一步（改映射表的步骤里写该表的约束，而不是集中到"注意事项"节）。幸存的清单条目必须带后果——"否则订得上、收不到"是牙齿，没有后果的规则是考据。嵌位还包括负面指引："一般不用改 A / B / C"一行字，省掉乱改和瞎看。

**why 分级。**前提级 why（拿掉它方案看起来就是错的，如"差值时常数项抵消"）进正文；参数级 why（"心跳 20s 是 60s 的三分之一"）留在常量定义处即代码注释；历史级 why（为什么否决某备选）进 ADR 或 commit message。判据：读者缺了这条 why 会不会误推导或怀疑模型？不会，砍。

**语域。**标题与正文用专业名词、专业表达：禁疑问句、反问句（标题"斜率怎么算"应为"斜率递推"）；禁口语比喻（"两台计算器"应为"两套复权口径"；"先问锚点这单你能接吗"应为"先校验锚点可用性"）。生动的点睛句可以留在正文一句，不进标题。

**媒介默认值。**

- 图表优先 Mermaid，但**图要挣位置**：生命周期、分支判定、拓扑才画图；一行文字能说清的不画。总图全文唯一，紧跟背景、先于所有机制节；机制节内仅在结构确属二维时加局部图。
- **片段即证词**：先有论断后选片段，片段必须能证明其旁的论断，对不上就换片段或改论断。单段 ≤ 50 行（能短则短——长代码块是"讲"，拆出流程块才是"演"）。
- **片段必须自解释**（摘录脱离上下文，靠注释补足理解）：
  - 注释附着在被解释的代码行上，写「这一段在做什么、为什么」；来源定位写在片段前的正文里（如「入口：`protobuf/protocolbuf.ts`」），不写进片段头部注释；
  - 不复述代码（「// 设置最低价」是噪音）；源码自带注释原样保留，不叠加矛盾注释；
  - 省略显式标注（`// …` 或「节选有删改」），不静默截断；
  - 嵌入真实代码而非转述——转述会静默漂移，引用的片段会响亮地漂移（代码改了、文档没改，diff 立刻可见）；若需大量注释才可懂，说明选段不当：换更自解释的区间，或改为示意流程块。
- 行内定位只用「文件 + 符号名」，**禁止行号**（行号会腐烂，符号名不会）。
- 流程块里嵌**可验算的样例值**（如"收盘 = 398.50 + 增量"，随后给一行数值演算），可验算的例子是埋在文档里的测试用例。

**强调预算。**加粗只给不变量与后果；表格给对比与映射；全是重点等于没有重点。

## 第 5 步：删减

用不对称账本逐句过：**这句话挂在读者的哪个动作上？**挂不上的删，删到疼为止。高频杂质清单：代码复读（读代码十秒能恢复的）、密排长段落（本可分条/表格/代码的信息写成三行以上散文——密度幻觉：行数少了认知税反而升）、无后果的参数罗列、装饰性图表、同一事实的二次陈述、离题漂移（坑清单混进无关领域的问题）、静态索引、元信息超两行、导出噪音（多余分隔线、乱码实体——说明没有以读者身份重读过）。

删完后以**读者**（不是作者）身份通读一遍：每次"想往上翻"是结构信号（东西放错了位置），每次"这句在干嘛"是杂质信号（在讲而不是在演）。

## 第 6 步：发文前自检

依次过一遍，任何一条不过回去修：

1. 标题通读 = 一个完整的故事？
2. 全文一个体裁、一个阅读时刻？
3. 每句话挂在读者的动作上？能从代码十秒恢复的已删？
4. 有没有超过三行的段落？并列 / 对比 / 确定逻辑 / 拓扑信息是否都装在对应容器里？
5. 图挣了位置且用 Mermaid？代码片段能证明其旁论断、单段 ≤ 50 行且自解释（行内解释注释到位、省略已标注）？无行号？
6. 规则都嵌进了会被违反的那一步？幸存清单条目都带后果？
7. why 分级归位（前提级在正文，参数/历史级回了家）？
8. 零元信息（基线 / 需求出处 / 读者画像；治理声明如"本文取代 X"除外）？无手写目录页、无静态索引表？
9. 高危句（数字、全称量词）逐条对到源头？示例值标了"示意"？
10. 运行符号巡检：`scripts/check_symbols.sh <文档路径> <仓库根>`（依赖 bash / git / grep / sed / find，Windows 下用 Git Bash；无 bash 环境时按脚本头部注释的逻辑人工核对）——对输出的 NOT FOUND 逐条判断：是概念词/仓库外引用则忽略，是真实符号名则修正文档或代码。
11. 若本篇与已有文档主题重叠：在新文档头部声明权威关系（"本文取代 X"），并同步在旧文档头部加被取代标注，或建议删除旧文档。一个主题只有一份正典。
12. 文件名通过 `references/naming.md` 的四项验收测试（排序 / 搜索 / 读名知文 / 腐烂）？

## 参考文件索引

- `references/genre-zhaji.md` — 札记深度指南（第 1 步选定札记后读）
- `references/genre-fangan.md` — 方案深度指南（选定方案后读）
- `references/genre-jiaocheng.md` — 教程深度指南（选定教程后读）
- `references/antipatterns.md` — 反模式与好坏对照（重写/评审/压缩前读）
- `references/doc-governance.md` — 文档经营：权威序、所有权、写不写的 ROI、答疑回写、AI 时代注意、安全边界（涉及团队文档治理问题时读）
- `references/naming.md` — 文件命名：检索键公式、四项验收测试、禁止项（创建或重命名文档时读）
